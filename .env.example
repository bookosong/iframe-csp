# metaso-proxy-autosearch 环境配置示例
# 复制此文件为 .env 并根据需要修改配置

# 基础配置
NODE_ENV=development
PORT=10101

# 可选：明确指定完整的服务器基础URL
# 如果不设置，系统会自动检测
# SERVER_BASE_URL=http://localhost:10101

# ========== 生产环境配置示例 ==========
# NODE_ENV=production
# PORT=80
# SERVER_BASE_URL=https://your-domain.com

# ========== Docker 配置示例 ==========
# NODE_ENV=production
# PORT=3000
# # Docker环境中通常不需要设置SERVER_BASE_URL

# ========== 开发环境自定义端口示例 ==========
# NODE_ENV=development
# PORT=8080
# SERVER_BASE_URL=http://localhost:8080
# 
# 配置示例:
# 
# 开发环境:
# NODE_ENV=development
# PORT=10101
# HOST=localhost
# PROTOCOL=http
# 结果: http://localhost:10101
#
# 生产环境:
# NODE_ENV=production
# PORT=443
# HOST=proxy.yourdomain.com
# PROTOCOL=https
# 结果: https://proxy.yourdomain.com:443
#
# 如果使用标准端口(http:80, https:443)，端口号会自动省略

# 注意事项:
# 1. 生产环境需要配置SSL证书以支持HTTPS
# 2. 确保防火墙允许配置的端口访问
# 3. 如果使用代理或负载均衡器，请相应调整配置
